---
title: Introduce Agent Versioning and lifecycle management
dek: Versioned agents can be validated and released in clinical trials safely because they can be rolled back.
facts:
  - label: Duration
    value: 1 month (end-to-end)
  - label: Role
    value: Lead product designer
  - label: Team
    value: EPD (1 Des, 1 PM, 3 Dev)
chapters:
  - id: problem
    label: Problem
  - id: framing
    label: Framing
  - id: ideation
    label: Ideation
  - id: constraint
    label: Constraint
  - id: refinement
    label: Refinement
  - id: outcome
    label: Outcome
hero:
  before: ../../assets/work/agent-versioning/00-hero-before.png
  after: ../../assets/work/agent-versioning/00-hero-after.png
  beforeAlt: The agent canvas before versioning — a single editable draft, with Save and Publish and no version history
  afterAlt: The same canvas after versioning — an unpublished-changes banner, a draft state control, version history, and accent borders on the nodes carrying updates
  # x/y are percentages of the frame, and mark the centre of the numbered pin.
  # Both shots are 1440x840 and the frame takes its ratio from them, so nothing
  # is cropped and these map straight onto the images. Read off the annotated
  # reference exports: each pin's square corner points back at the control it
  # explains, so the centre sits down and to the right of that control.
  beforeNotes:
    - x: 22.6
      y: 14.8
      text: To make changes, user had to manually change state to draft
    - x: 69.6
      y: 15.5
      text: Any new changes overwrote the previous agent configuration
    - x: 54.2
      y: 15.5
      text: Inconsistent navigation pattern for accessing agent's settings
  afterNotes:
    - x: 25.6
      y: 21.0
      text: State changes are triggered contextually based on relevant actions
    - x: 35.4
      y: 46.5
      text: All changes are versioned and viewable in the versions modal
    - x: 54.9
      y: 12.7
      text: User receives a visible state change signifier that they can dismiss
    - x: 89.0
      y: 16.7
      text: Agent settings are accessible in a side panel like the rest of the nodes on canvas
actions:
  primary:
    label: View final solution
    href: '#outcome'
  secondary:
    label: Read case study
    href: '#story'
---

import Section from '../../components/case-study/Section.astro';
import Figure from '../../components/case-study/Figure.astro';
import FigureRow from '../../components/case-study/FigureRow.astro';
import NoteBox from '../../components/case-study/NoteBox.astro';
import VideoFigure from '../../components/case-study/VideoFigure.astro';
import Reflection from '../../components/case-study/Reflection.astro';
import Term from '../../components/case-study/Term.astro';

import problemCanvas from '../../assets/work/agent-versioning/01-problem-canvas.png';
import studyStatusChange from '../../assets/work/agent-versioning/02-problem-status-change.png';
import lifecycleMap from '../../assets/work/agent-versioning/03-framing-lifecycle-map.png';
import stateIterations from '../../assets/work/agent-versioning/04-ideation-state-iterations.png';
import dependencyCardIterations from '../../assets/work/agent-versioning/05-ideation-dependency-card.png';
import mandatoryPublish from '../../assets/work/agent-versioning/06-ideation-mandatory-publish.png';
import versionsModal from '../../assets/work/agent-versioning/07-ideation-versions-modal.png';
import visualCues from '../../assets/work/agent-versioning/08-ideation-visual-cues.png';
import scopeSidePanel from '../../assets/work/agent-versioning/09-constraint-side-panel.png';
import scopeModal from '../../assets/work/agent-versioning/10-constraint-modal.png';
import versionCardFix from '../../assets/work/agent-versioning/11-refinement-version-card.png';
import outcomeStill from '../../assets/work/agent-versioning/12-outcome-still.png';

<Section chapter="problem" eyebrow="Sales blocked" heading="There was no way to validate the changes made to an agent and release it safely in the workspace">
  <Figure slot="visual" src={problemCanvas} alt="The Agent Studio canvas with a draft agent, a chat message trigger and a connected app" />

  Agent Studio's canvas is where an <Term id="ai-builder">AI builder</Term> assembles an
  agentic workflow. For eg, a <Term id="cra">Clinical Research Associate</Term> (CRA)
  responsible for monitoring a set of trials can build an agent, connect it to their
  organisation's data sources, and put it to work without involving engineering.

  Currently, editing an agent simply overwrote it and the changes went live immediately. As a
  result, there was no way to implement any governance which allowed customers to validate the
  changes before releasing it.

  This was blocking implementation of agent studio at big pharma companies and CROs.
</Section>

<Section eyebrow="Studying existing platform patterns" heading="Study studio's status change mechanisms didn't apply to agents">
  <Figure slot="visual" src={studyStatusChange} alt="Study Studio's change-status dialog, showing a current status of Draft moving to Publish" />

  Before diving into ideation, I studied an existing pattern for status change in
  <Term id="study-studio">Study Studio</Term> (another product in the Medable platform) to see
  if I can use it in the context of agents. I found it inadequate for agents because…

  1. **More UI manoeuvring:** A builder will receive updates on dependencies frequently. To
     change the status first, and then accept the new update would be intrusive UX.
  2. **Misleading Mental modal:** Changing status from published to draft doesn't unpublish the
     agent (unlike the study, which becomes unavailable). For agents, changing state to draft is
     expected to create an identical version of the agent which user can modify, while the
     previous version stays published and available for use simultaneously.
</Section>

<Section chapter="framing" eyebrow="Builder's requirements" claim heading="Agent builders should be able to view updates, accept the ones they want (except the mandatory ones) and roll back to a previous version">
  <Figure slot="visual" src={lifecycleMap} alt="A lifecycle map: draft to published v1 on first publish, then edit to draft and second publish to published v2" />

  I began by mapping how versioning will change the existing workflow for AI builders based on
  my product knowledge of agent studio.

  After versioning, whenever a <Term id="dependency">dependency</Term> is modified, the agent
  using it receives an update. I arrived at the following requirements for them.

  1. They should be able to view it
  2. They should be able to accept it only if they want to
  3. In case the update is mandatory, they shouldn't be able to update the agent without
     accepting it
  4. They should be able to rollback to a previous agent version
</Section>

<Section chapter="ideation" eyebrow="Designing new agent lifecycle patterns" claim heading="Version number is irrelevant. To help user track agent's lifecycle, anchor their attention to state">
  <Figure slot="visual" src={stateIterations} alt="Three iterations of the agent header comparing published and draft states, with the selected iteration at the bottom" />

  While ideating how to visualise agent's lifecycle, I realised the version number is not
  important. The version can be v237. Therefore, user's mental model should be anchored in
  canvas state so that it's clear which actions are allowed.

  Therefore, the experience of versioning boils down to helping the user:

  1. identify which state they are in currently, and as a result what they can do
  2. clearly show when the state has changed
  3. understand they have to republish for changes to take effect
</Section>

<Section eyebrow="Designing the dependency card component to handle updates uniformly" claim heading="Users want to know what changed in the update and both the previous and the latest version number">
  <Figure slot="visual" src={dependencyCardIterations} alt="Three iterations of the dependency card, from a simple accept/dismiss affordance to the selected version-aware design" caption="Iterations 1 to 3. The third is the selected iteration." />

  The dependency card was already crucial for editing the configuration of a dependency. Now
  versioning was adding another layer of complexity.

  To keep it simple, I began with a simple Y/N affordance to accept of dismiss updates
  (iteration 1). But on viewing it with the user lens, it fell short because it didn't…

  1. give any info to decide whether the new update was important
  2. afford editing while it was also showing an update
  3. accommodate multiple files under each dependency type

  These points became the requirements for the 2nd iteration. But now the component felt too
  busy. So for iteration 3, I took out the current version thinking it didn't add any value.

  But on user testing iteration 3 with internal users, I learnt that they confused the version
  number shown to be the existing version, not the new version. This was rectified in the final
  iteration, which handled all requirements and scaled to all dependency types.

  <NoteBox title="Note on user testing">
    Because conducting user tests with end users (eg, CRAs) can take significantly longer to set
    up, and we were on the clock with versioning, I validated these concepts with 5 internal
    users in the client services team who use the product daily.
  </NoteBox>
</Section>

<Section eyebrow="Enforcing mandatory updates" claim heading="Bring it up at the moment of publish, otherwise it adds unnecessary cognitive load">
  <Figure slot="visual" src={mandatoryPublish} alt="A dialog listing dependencies with mandatory updates, blocking publish until all are accepted" />

  <Term id="mandatory-update">Mandatory updates</Term> are crucial for security, and made by
  workspace admins, hence builders have to accept it.

  The key UX driver here was *when* to bring the mandatory updates up and doing it without being
  intrusive. If the user doesn't accept them on the canvas, instead of highlighting it, the more
  thoughtful way to enforce it would be to bring them up at the point of publish so that the
  user can accept them all in one shot.

  I also debated whether mandatory updates should be highlighted on the canvas, exploring
  various layouts like accordions and grids to list them. Finally, I decided the optimal UX was
  to accept all mandatory updates in one go at the time of publish. A carousel works best
  because it does not force the user to see all the updates, yet does not prevent them to go
  through them if they want — <Term id="progressive-disclosure">progressive disclosure</Term>.
</Section>

<Section eyebrow="Showing versions and rollback" claim heading="Modal can handle large number of versions. Arranging the version configuration in tabs keeps it easy to find for the user">
  <Figure slot="visual" src={versionsModal} alt="The versions modal: a dated list of versions on the left, the selected version's configuration in tabs on the right" />

  Regarding versions, I knew the list could grow long, that's why I opted to navigate user to a
  modal, where I can show large numbers of versions.

  The meta data on version card was decided based on what users want to know—when was the
  version created and by whom.

  At first, I decided not to put the "published" tag assuming it would be obvious that the
  version on top of the pile is the published, but during stakeholder discussion, I realised the
  topmost version could be draft.

  The content of each version was arranged in tabs for the following reasons:

  1. make it easy for users (likely auditor or builder) to find what they are looking for
  2. to avoid long scroll
</Section>

<Section eyebrow="Designing visual cues for state change and updates" claim heading="Use colour and motion to direct user's attention to nodes with updates and state changes subtly">
  <Figure slot="visual" src={visualCues} alt="An accent-bordered node, a floating updates-available tab, and a banner reading 'This canvas contains unpublished changes'" />

  I wanted to convey important information to the user about updates and state changes on the
  canvas in a subtle way without demanding too much cognition from them. I achieved this using…

  1. accent border to visually differentiate nodes with updates, making it easier to scan for
     updates.
  2. a floating tab to convey total updates and accept all updates in one go.
  3. a banner moving at the top when canvas changes to draft, clearly giving feedback of state
     change.
</Section>

<Section chapter="constraint" eyebrow="Meeting engineering constraints" claim heading="To meet the release date, I reduced scope by cutting the side panel because even though it was better UX, it wasn't crucial to versioning">
  <FigureRow slot="visual">
    <Figure src={scopeSidePanel} alt="The settings side panel, consistent with the dependency card design" caption="Side panel (consistent)" />
    <Figure src={scopeModal} alt="The edit-agent-details modal, inconsistent with the dependency card design" caption="Modal (inconsistent)" />
  </FigureRow>

  Engineering assessed the complete design (including the side panels) would take longer than
  the release deadline. Instead of pushing the dates, I was asked for a middle ground.

  They agreed the side panel was better UX, but because it involved new components,
  implementing it within the deadline wasn't possible.

  So I cut the settings side panel designed to fix consistency issues because it wasn't crucial
  to versioning. Consequently, I had to redesign the dependency card again to fit it to the
  inconsistent modal design.
</Section>

<Section chapter="refinement" eyebrow="Post launch refinement" claim heading="Shipped a fix to a UI bug by designing it directly in code">
  <Figure slot="visual" src={versionCardFix} alt="A merged pull request redesigning and unifying the version timeline card, with the old and new designs side by side" />

  Post launch feedback revealed the version cards were breaking in production because
  double-digit version numbers were bleeding out of their borders. Even the draft tag looked too
  cramped.

  I didn't involve devs because this was a purely frontend change. I opened a branch in cursor,
  tried a few iterations directly in code, finalised one and raised a merge request in under an
  hour.
</Section>

<Section chapter="outcome" id="outcome" eyebrow="Outcome" claim heading="Designed the versioning UX, including fixing UI bugs found in production, in a month to meet business goals" layout="wide">
  Shipped versioning in a month to meet the release deadline, fixing UI bugs found in
  productions and alleviating compliance concerns expressed by existing and potential customers
  alike.

  <VideoFigure
    slot="visual"
    poster={outcomeStill}
    src="/media/agent-versioning-walkthrough.mp4"
    alt="Walkthrough of the shipped versioning experience on the agent canvas"
  />
</Section>

<Reflection>
  From the next time when meeting a deadline forces me to compromise on design, I'll get
  explicit agreement from PM to pick it up for the following release, with a ticket raised, so
  that the better UX stays visible.

  I also got to experiment with designing in code in this project. Based on this experience, I
  have the following thoughts:

  - I feel designing in code is not suited for exploring diverse concepts; pen on paper is still
    the fastest
  - On the contrary, it shines when it comes to small problems with known solution patterns
  - It will empower design teams to own more of the frontend

  At the end of the day, it is another tool in a designer's belt which they can reach for
  depending on the situation.

  Oh, I also learned about code reviews and testing. I reviewed my code and took screenshots for
  testing, getting a small taste of developing and pushing something end to end.
</Reflection>
