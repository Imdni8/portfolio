---
title: Made agent actions easier to follow
industry: Clinical trials
technology: AI Agents
year: 2026
thumbnail:
    src: ../../assets/work/audit-logs/00-thumbnail.png
    alt: The redesigned audit log page on a laptop — a colour-coded activity timeline beside a details panel showing a version change
order: 20
roles:
    - kind: design-type
      label: Redesign
aiFeature: true
facts:
    - label: Duration
      value: 3 weeks
    - label: Role
      value: Lead product designer
    - label: Team
      value: EPD (1 Des, 1 PM, 3 Dev)
chapters:
    - id: introduction
      label: Introduction
    - id: problem
      label: Problem
    - id: goals
      label: Goals
    - id: solution
      label: Solution
heroShot:
    src: ../../assets/work/audit-logs/00-hero.png
    alt: The redesigned audit log page — a version-change side panel next to the activity timeline, with search converted to filter tags
---

import Chapter from "../../components/case-study/Chapter.astro";
import StoryChapter from "../../components/case-study/StoryChapter.astro";
import VideoFigure from "../../components/case-study/VideoFigure.astro";
import AnnotatedFigure from "../../components/case-study/AnnotatedFigure.astro";
import GoalChip from "../../components/case-study/GoalChip.astro";
import StatTile from "../../components/case-study/StatTile.astro";
import Slide from "../../components/case-study/Slide.astro";
import SlideVideo from "../../components/case-study/SlideVideo.astro";
import SlideCallout from "../../components/case-study/SlideCallout.astro";

import previousAuditPage from "../../assets/work/audit-logs/01-problem-initial-audit-page.png";
import problemLogEntries from "../../assets/work/audit-logs/21-problem-log-entries.png";
import problemExpandedLogView from "../../assets/work/audit-logs/22-problem-expanded-log-view.png";
import problemInconsistentFilters from "../../assets/work/audit-logs/23-problem-inconsistent-filters.png";
import problemAutomaticSearch from "../../assets/work/audit-logs/24-problem-automatic-search.png";

import slideLogResults from "../../assets/work/audit-logs/slide-1-log-results.jpg";
import slideDbActions from "../../assets/work/audit-logs/slide-3-db-actions.jpg";
import slideFilterByResource from "../../assets/work/audit-logs/slide-4-filter-by-resource.jpg";
import slideAiSummary from "../../assets/work/audit-logs/slide-5-ai-summary.jpg";

{/* Same column the narrow chapters below read their heading/intro at —
    --measure-copy with the gutter outside the cap (CLAUDE.md's page-frame
    formula) — so the eyebrow and the stat cards line up with "Introduction"
    and its prose rather than with the wider --measure-story chapters use
    for their own media. */}
<section class="mx-auto max-w-[calc(var(--measure-copy)+2*var(--gutter))] px-[var(--gutter)] pt-20">
  <p class="type-overline text-center">Outcome</p>
  <div class="mt-6 grid gap-[var(--spacing-xl)] grid-cols-[repeat(auto-fit,minmax(16rem,1fr))]">
    <StatTile size="lg" value="26%" label="in usability (measured by SUS going from 57 to 72)" />
    <StatTile size="lg" value="48%" label="in weekly searches after one week of implementation" />
  </div>
</section>

{/* The rule sits inside the same gutter-safe frame as the section above it
    (page-frame formula again) so its ends line up with the cards' own
    edges, rather than a bare max-width that would draw it wider than the
    content it's dividing. */}
<div class="mx-auto mt-[50px] max-w-[calc(var(--measure-copy)+2*var(--gutter))] px-[var(--gutter)]">
  <hr class="border-t border-border" />
</div>

<Chapter id="introduction">
<StoryChapter heading="Introduction" narrow>
<Fragment slot="intro">

Agent action is a record of changes made to agents. They are recorded because
each change could affect the way agents behave. So when an agent behaves
unexpectedly, these actions help track which change introduced a bug and undo
it.

</Fragment>

<VideoFigure poster={previousAuditPage} src="/media/audit-logs-initial-walkthrough.mp4" alt="Walkthrough of the original, pre-redesign agent action page — a flat list mixing user and database actions with no grouping" caption="Previous version of agent actions" />

</StoryChapter>
</Chapter>

<Chapter id="problem">
<StoryChapter heading="Problem" narrow>
<Fragment slot="intro">

The first version of agent action logs (also called logs) were hard to
understand, **creating compliance risk** because it took longer to troubleshoot
agents. The core issues contributing to this, in the order of severity,
were:

</Fragment>

<div class="grid gap-16">
  <AnnotatedFigure
    src={problemLogEntries}
    alt="Two audit log entries for the same agent — an update and its creation — each identifying the agent only by its raw database key, 'Agent #55345'"
    notes={[
      { x: 4, y: 28, label: "1", text: "All agent actions (create, update, delete) looked the same visually, making it hard to scan for a particular action type." },
      { x: 23, y: 77, label: "2", text: "The agent names used database IDs instead of human readable names given by the user." },
      { x: 51.5, y: 49, label: "3", text: "The list also included database actions due to the backend architecture, causing confusion." },
    ]}
  />
  <AnnotatedFigure
    src={problemExpandedLogView}
    alt="An expanded log entry showing its full field-diff table, pushing the rest of the page down"
    notes={[
	  { x: 4.7, y: 19.3, label: "4", text: "Expanding each action log covered the whole screen, making it difficult to view and compare the content of adjacent logs." },
      { x: 10.8, y: 74, label: "4", text: "Expanding each action log covered the whole screen, making it difficult to view and compare the content of adjacent logs." }
    ]}
  />
  <AnnotatedFigure
    src={problemInconsistentFilters}
    alt="The filter row's Clear filters link above a separate 'Filtering by resource: Agent #50635' tag"
    notes={[
      { x: 9.3, y: 60.5, label: "5", text: "Search and agent filters looked and worked differently. Not directly related to agent logs but it made the experience inconsistent and the blue bar took unnecessary space." },
      { x: 25, y: 92, label: "5", text: "Search and agent filters looked and worked differently. Not directly related to agent logs but it made the experience inconsistent and the blue bar took unnecessary space." },
    ]}
  />
  <AnnotatedFigure
    src={problemAutomaticSearch}
    alt="The Audit page on arrival — From, To, User, Action, Resource and Show filters already filled with defaults, and a spinner in the empty results table while the automatic search runs"
    notes={[
      { x: 96.9, y: 37.7, label: "6", text: "Page was set to automatic search, wasting user's time because they had to wait for the previous search to finish first before initiating the search they want." },
    ]}
  />
</div>

</StoryChapter>
</Chapter>

<Chapter id="goals">
<StoryChapter heading="Goals" narrow>
<Fragment slot="intro">

Based on the research, I arrived at the following goals for the redesign
project. How might we...

</Fragment>

{/* -mt-8 claws StoryChapter's 48px intro step back to 16px: the tiles finish the "How might we..." sentence. */}
<div class="mx-auto -mt-8 grid w-full max-w-[var(--measure-copy)] gap-4 grid-cols-[repeat(auto-fit,minmax(14rem,1fr))]">
  <GoalChip>Make it easier to search for the right agent actions</GoalChip>
  <GoalChip>Make the data change easy to understand</GoalChip>
  <GoalChip>Make it easy to compare with adjacent logs</GoalChip>
  <GoalChip>Help differentiate database actions from agent actions</GoalChip>
  <GoalChip>Eliminate initial lag from automated searching</GoalChip>
</div>

</StoryChapter>
</Chapter>

<Chapter id="solution">
<StoryChapter heading="Solution" narrow>
<Fragment slot="intro">

I redesigned the entire module, from initiating search to visualising
database actions differently, making agent actions easier to follow. This
led to...

</Fragment>

  <Slide static>
    <SlideVideo
      slot="media"
      src="/media/audit-logs-log-results.mp4"
      poster={slideLogResults}
      alt="The audit log list showing timestamp-led rows with action-type icons, a colour-coded left border, and a shrunken search bar"
      pin="bottom-right"
    />
    <SlideCallout marker="1">Easier to find the right agent action due to colour coded logs and leading with time</SlideCallout>
  </Slide>
  <Slide static>
    <SlideVideo
      slot="media"
      src="/media/audit-logs-ai-summary.mp4"
      poster={slideAiSummary}
      alt="Generating an AI summary of a log's data change inside the details side panel"
      pin="bottom-left"
    />
    <SlideCallout marker="2">Informative side panel with AI summarisation</SlideCallout>
  </Slide>
  <Slide static>
    <SlideVideo
      slot="media"
      src="/media/audit-logs-db-actions.mp4"
      poster={slideDbActions}
      alt="Expanding a 'Show db actions' divider to reveal the grouped database action entries it was hiding"
      pin="bottom-right"
    />
    <SlideCallout marker="3">Suppressed database actions to declutter agent logs list</SlideCallout>
  </Slide>
  <Slide static>
    <SlideVideo
      slot="media"
      src="/media/audit-logs-filter-by-resource.mp4"
      poster={slideFilterByResource}
      alt="Filtering the audit trail by resource using a control consistent with the rest of the platform"
      pin="bottom-right"
    />
    <SlideCallout marker="4">Consistent filtering experience</SlideCallout>
  </Slide>

</StoryChapter>
</Chapter>

{/* Everything below is the pre-redesign body — kept for reference, not
    deleted, while the new cut above replaces it on the page. Same
    convention the Outcome/Reflection block already used further down in
    this file before this change.

    Restoring any of this means putting its imports back at the top of the
    file: StorySection, ReadInDetail, StoryBlock, Figure, NoteBox,
    VideoFigure, Term, plus every named image import only these blocks
    reference (initialAuditPage, verticallyExpandingLogs,
    inconsistentSearchFilters, participantWorkflow, grafanaDashboard,
    earlierSearchConcept, dbActionsExpanding, explicitSearchAction,
    searchTags, aiChatConcept, aiSummarisationConcept, versionChips) — all
    stripped from the live import list above since nothing the compiler sees
    is referencing them once this comment wraps them.

<StorySection heading="Problem" divider split="even">

> Incomprehensible agent audit trail led to longer times for fixing agents and more effort to audit, creating compliance risk

Audit logs are a regulated artifact. When a sponsor or auditor asks what an agent did and when, the log is the evidence. If it causes confusion, the trial data can be flagged as unfit for use, creating compliance risk and other unwanted effects downstream.

<VideoFigure slot="visual" poster={initialAuditPage} src="/media/audit-logs-initial-walkthrough.mp4" alt="Walkthrough of the initial version of the audit log page, listing raw entries per agent with no grouping or visual hierarchy" caption="Initial version of the audit logs" />

</StorySection>

<StorySection heading="Refining based on user testing" split="even">

> Appending version number improved user's ability to compare logs, and follow audit trails better

Once implementation was complete, I user-tested the design again with 5 internal users. The overall SUS came to 72 after the redesign.

Also, during user testing, I observed many users opened the side panel to check the version of the agent. Upon asking why, they said knowing the version number helps them compare the logs faster and figure out exactly which change caused the problem.

<NoteBox title="Note on user testing">
Because the turnaround cycles were fast (1-week sprints), I couldn't get feedback from end users. Not having any insight from the regulator persona is a known risk that I've flagged to the team.
</NoteBox>

<Figure slot="visual" src={versionChips} alt="The log list with a version chip such as v1 or v2 shown beside each resource name" caption="Appending version to agent helps user follow changes easily" />

</StorySection>

<ReadInDetail>

<StoryChapter heading="Understanding the problem">

<StoryBlock title="UX Audit">

<Figure slot="visual" src={verticallyExpandingLogs} alt="A log expanded to show its changed fields, pushing every log beneath it further down the page" caption="vertically expanding logs" />
<Figure slot="visual" src={inconsistentSearchFilters} alt="The search bar showing a resource filter applied as a separate, inconsistent control" caption="inconsistent search filters" />

> Hard to follow the audit trail because of database actions mixed with user actions, vertically expanding logs and poor visual hierarchy

1. Audit page first load lagged severely because opening it triggered a search automatically in the background (confirmed in the network tab of dev tools). As a result, users couldn't change the search criteria because the UI would be unresponsive for some time.
2. The search results contained user actions and database actions together in one list. So one user action appeared as multiple log entries.
3. Each log needed to be expanded twice to see changed fields, which pushed the logs below further down. This was detrimental to the task because users often need to see multiple logs together for comparing data changes.
4. Filtering by a resource clashed with the initial search filters.
5. The search section wasn't fixed, forcing the user to scroll back up if they wanted to reset the search criteria or refresh to view the latest logs.
6. The log list was hard to scan because of poor visual hierarchy.

</StoryBlock>

<StoryBlock title="User research">

<Figure slot="visual" src={participantWorkflow} alt="Illustrative screenshot of a participant sharing their screen on a video call while walking through the audit log page" caption="Participant explaining their workflow on a video call [illustrative image]" />

I conducted contextual inquiry with five internal builders in the professional services team to understand how they use audit logs for troubleshooting.

My finding was that there is an order to how they locate logs, after filtering for the right resource:

1. Because they have a date and time after which the agent started malfunctioning, they first look to find logs around that time
2. After locating the correct time period, they go through each log to see what data was modified
3. They also look at adjacent logs to get the full picture

After each session, I also gave each participant a <Term id="sus">System Usability Scale</Term> (SUS) questionnaire to establish benchmark usability of the current system. The average SUS for this version came to 57.

</StoryBlock>

</StoryChapter>

<StoryChapter heading="Ideation">

<Fragment slot="intro">

Based on the research, I synthesised the project into five goals

1. Help users locate the right log
2. Make the data change inside a log easy to understand and compare against adjacent logs
3. Differentiate database actions from user actions
4. Optimise layout to fit more logs vertically
5. Eliminate initial lag from unintentional log searching

</Fragment>

<StoryBlock title="Help identify the right logs">

<Figure slot="visual" src={grafanaDashboard} alt="Grafana's log dashboard, leading every row with a timestamp" caption="Screenshot of Grafana's log dashboard" />
<Figure slot="visual" src={earlierSearchConcept} alt="An earlier design concept with a search bar and a details panel for filtering logs by time" caption="Earlier design concept with search and filtering" />

> Timestamps in the beginning make it easier to locate the right logs

Logs being a core part of many developer applications (e.g. Grafana), I began by studying existing examples. From Grafana's log design, I picked the timestamp-led pattern because it helps users ignore other details and locate the right group of logs by time.

But instead of surfacing the technical details right at the top level and making it information dense, I chose to expose the underlying database changes of the log in the side panel.

Earlier design direction allowed searching and filtering by time period to locate logs faster, but I had to drop it because the results were fetched in batches, so both searching and filtering would happen on a subset of the data.

</StoryBlock>

<StoryBlock title="Handle audit trail cluttering due to database actions">

<Figure slot="visual" src={dbActionsExpanding} alt="Four separate log entries — created, updated, updated, created — all for the same prompt, three of them sharing one timestamp" caption="One user action created four logs" />

Users expect to see one log for one action, even if that one action included multiple changes. For example, if an agent's prompt and skill are updated and republished, the action of publishing should occur once in the logs.

But due to how the database was architected, one user action could generate multiple logs. This confused users.

At first, we discussed hiding the database actions. But that could become a compliance issue. So after discussing feasibility with developers, I introduced the idea of "database actions" and designed logs to visually differentiate between database actions and user actions.

</StoryBlock>

<StoryBlock title="Log Search redesign">

<Figure slot="visual" src={explicitSearchAction} alt="A redesigned search control with an explicit Search button instead of searching automatically on load" caption="Added an explicit search action to prevent automatic search from causing lag" />
<Figure slot="visual" src={searchTags} alt="Search criteria collapsed into removable tags in a fixed header row" caption="Search criteria converted to tags to allow more vertical space" />

Another problem was to improve how log search was initiated. First, I added an explicit button to prevent automatic searching and the lag it caused on initial page load.

After executing the search, the fields changed to tags so that:

1. Vertical space can be maximised to fit more logs
2. When a user filters by a resource, the tags system can be extended so that filtering stays consistent

</StoryBlock>

<StoryBlock title="Summarising logs">

<Figure slot="visual" src={aiChatConcept} alt="Concept 1 — a chat panel that lets the user ask follow-up questions about the log" caption="Concept 1: AI-chat to support follow-up questions" />
<Figure slot="visual" src={aiSummarisationConcept} alt="Concept 2 — a Summarise action that writes a plain-language summary directly into the details panel" caption="Concept 2: AI summarisation" />

Sometimes the jargon used in the data changes of a log is not understandable to users. To overcome this, I explored two AI-summarisation directions.

Concept 2 was selected because we weren't sure if follow-up chats were necessary. We decided to ship the simpler version and iterate based on feedback, intentionally ignoring the edge cases for handling wrong summary generation.

</StoryBlock>

</StoryChapter>

Outcome and Reflection are dropped from the page for now, matching
    agent-versioning's current WIP state — neither appears in the latest
    design reference for this page. Kept below, in their old pre-redesign
    Section shape so the content isn't lost; restore once there's a design
    for how they fit the new cut. (This note used to be its own separate
    comment block before the rest of this file's body joined it inside one
    outer comment — left unmarked here rather than nested, since MDX
    comments cannot nest: a second closing delimiter would end the outer
    comment early and the remaining old content below would render as live,
    broken JSX.)

    Restoring one also means putting its imports back at the top of this file:
    Section, Reflection, finalDesignThumbnail and reflectionAuditFlow were
    removed with the rest of the dead imports, since nothing the compiler
    sees was referencing them (a commented block is discarded before any of
    it is resolved, so no tooling here would ever have flagged them).

<Section chapter="outcome" id="outcome" claim eyebrow="Outcome" heading="Improved the audit logs design as measured by increase in SUS from 57 → 72" layout="wide">
  <VideoFigure slot="visual" poster={finalDesignThumbnail} src="/media/audit-logs-final-design-walkthrough.mp4" alt="Walkthrough of the shipped audit log page" caption="Final design walkthrough" />

Redesigned audit logs to make it easier to find using timestamp and version number, understand data changes using AI-summarisation, and maximise the vertical space to fit more logs on the screen.

The new version was released on time and well received by the customer who had raised the original feedback.

</Section>

<Reflection>
  Next time, in addition to SUS, I would also measure an objective metric for benchmarking design improvement — for example, the time taken to find a log.

Also, since the workflow predominantly focuses on auditing one agent at a time, for the next iteration I'm going to propose a flow that can be initiated from the agent itself, so we can save the user the trip to the audit page and back. Something like this:

  <Figure src={reflectionAuditFlow} alt="An agent's overflow menu open with a new 'Audit' entry alongside Edit, Share and Setup monitor, pointing to the resulting audit logs modal scoped to that agent" caption="Audit logs open in a modal window from the agent's overflow menu" />
</Reflection>

</ReadInDetail>
*/}
