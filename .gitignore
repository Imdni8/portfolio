# build output
dist/
.astro/

# dependencies
node_modules/

# macOS
.DS_Store

# environment
.env
.env.local
.env.production

# editors
# `.vscode/*` rather than `.vscode/`, so the two shared files below can be
# un-ignored: git cannot re-include a path inside an ignored *directory*.
# Personal settings (launch.json, per-user overrides) stay ignored as before.
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/

# temp files
scratch/

# documentation
README.md
CLAUDE.md

# storybook build output
storybook-static/

# stray local-agent config picked up when a tool ran with an unexpanded `~`
# (writes relative to cwd instead of the real home directory) — see cwd/HOME
# handling in whatever ran `pi`.
~/

# Claude Code worktrees — each is its own git repo, nested under here.
# Accidentally got committed as gitlinks once already (see the commit that
# stops tracking them); ignoring the whole directory so it can't recur.
.claude/worktrees/
.vercel

# Added by `vercel link`, narrowed: the blanket `.env*` also matched
# `.env.example`, which is meant to be committed as the key's documentation.
.env*
!.env.example
